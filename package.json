{
  "devDependencies": {
    "@babel/plugin-transform-modules-commonjs": "^7.18.6",
    "@types/jest": "^29.0.0",
    "dotenv": "^16.0.1",
    "jest": "^28.1.3",
    "ts-jest": "^28.0.8"
  },
  "scripts": {
    "test": "jest",
    "build": "tsc --outDir ./dist",
    "build:dev": "tsc -w && cp ." 
  },
  "dependencies": {
    "module-alias": "^2.2.2",
    "node-fetch": "^2.6.7"
  }
}
